import { HighlightDevice } from './highlight-device';

describe('HighlightDevice', () => {
  it('should create an instance', () => {
    const directive = new HighlightDevice();
    expect(directive).toBeTruthy();
  });
});
